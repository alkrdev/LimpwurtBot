# Discord bot token, from https://discord.com/developers/applications
DISCORD_TOKEN=

# Application (client) ID, from the same page as the token
CLIENT_ID=

# Optional: set this during development to register commands to a single
# guild (instant updates). Leave empty to register commands globally
# (can take up to ~1 hour to propagate, use for production).
GUILD_ID=
